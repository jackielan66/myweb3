// components/ClientLeaks.tsx
'use client'
import { getSecret } from '@/lib/secret'

export default function ClientLeaks() {
    // 仅用于展示错误使用，不可以这么用
    // getSecret()
    
    return <div>client com</div>
}
