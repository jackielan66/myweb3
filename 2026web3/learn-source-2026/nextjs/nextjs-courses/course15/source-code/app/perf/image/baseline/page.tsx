import HeroImageBaseline from '@/components/HeroImageBaseline';

export default function Page() {
  return (
    <main style={{maxWidth: 860, margin: '0 auto', padding: 24}}>
      <HeroImageBaseline />
    </main>
  );
}