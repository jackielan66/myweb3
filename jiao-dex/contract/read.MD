用 hardhat 初始化项目

- 
hardhat 已经更新到3.0.0版本，
但是我们最好还是使用2.0版本
安装命令
-

```shell
npm install hardhat@2.22.3 --save-dev
```

启动node节点
```shell
npx hardhat node
```

创建合约
```shell
npx hardhat create:contract
```